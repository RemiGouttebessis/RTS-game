use bevy::prelude::*;

/// Asset loading and data-driven unit/building definitions (RON, under `data/`).
/// No systems yet.
pub struct GameAssetsPlugin;

impl Plugin for GameAssetsPlugin {
    fn build(&self, _app: &mut App) {}
}
